It is about the Form  which we have done in the js already.

## Router:-
    #.BROWSER ROUTER:- Uses History API and when we switch paths in React App It intercepts the request, keeps the browser from reloading, updates the URL in the address bar dynamically, and tells React to swap the components on the screen. It creates the perfect illusion of moving to a new webpage instantly.
