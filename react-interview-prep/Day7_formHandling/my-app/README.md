It is about the Form  which we have done in the js already.

## FORM HANDLING:-
    #. Whenever we submit the form it reloads the page it is its defalut behaviour so to prevent it we use e.preventDefault() function.
    #. How we handle form i suing any function and call it in form tag with onSubmit={} event listener.

## Two Way Binding to HAndle the values in Form
    #. we have to use useState and the value attribute in the input tag and pass the useState variable in it
    #. so we use value attribute and pass state variable to capture the name and to change it whenever it egt changes
